matrix:
  include:
    - language: python
      sudo: true
      python:
        - "3.7"
      before_install:
        - cd spy
      install:
        - bash init.sh
        - pip install -r requirements.txt
      script:
        - python Test.py